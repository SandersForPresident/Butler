describe('Bot', function () {
  var chai = require('chai'),
      sinon = require('sinon'),
      Bot = require('../../src/bot'),
      expect,
      should;
  before(function () {
    expect = chai.expect;
    should = chai.should;
  });
  it ('should do something', function () {
    // @TODO
  });
});
